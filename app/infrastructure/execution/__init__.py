"""
Инфраструктура исполнения ордеров (Execution Layer).

Этот пакет содержит реализацию адаптеров для исполнения торговых приказов
в реальной рыночной среде (Live Trading).

Роль в архитектуре:
    Классы этого пакета реализуют интерфейс `BaseExecutionHandler`, определенный
    в ядре (`app.core.interfaces`). Они отвечают за перевод внутренних событий
    системы (`OrderEvent`) в конкретные API-запросы к биржам.

Состав:
    - **live.py**: Содержит `LiveExecutionHandler` — основной класс для реальной торговли.
      Он отправляет ордера через API клиентов (из пакета `exchanges`) и подписывается
      на потоки обновлений (WebSockets/gRPC), чтобы получать подтверждения сделок (`FillEvent`).
"""