"""
Уровень доступа к данным (Database Layer).

Этот пакет отвечает за всё взаимодействие с базой данных:
от настройки подключения до ORM-моделей и репозиториев.

Архитектура:
    Используется **SQLAlchemy** в асинхронном режиме (`aiosqlite`).
    Реализован паттерн **Repository** для абстрагирования бизнес-логики от SQL-запросов.

Состав пакета:
    - **models.py**: Определения таблиц БД (BotInstance, StrategyConfig, etc.).
    - **repositories.py**: Классы для CRUD-операций (Create, Read, Update, Delete).
    - **session.py**: Настройка `async_session_factory` и движка БД.
    - **signal_logger.py**: Асинхронный сервис для сохранения истории сигналов.
"""