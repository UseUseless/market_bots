"""
Инфраструктура поставки рыночных данных (Data Feeds).

Этот пакет отвечает за загрузку, нормализацию и доставку рыночных данных
в ядро системы. Архитектура пакета разделена на два независимых направления:
работу с историческими файлами (Offline) и работу с сетью (Online).

Основные компоненты:
    - **LiveDataProvider**: Универсальный провайдер для реального времени.
      Объединяет REST API (история) и WebSocket (обновления).
    - **BacktestDataProvider**: Эмулятор потока данных. Итерируется по
      историческому DataFrame, скрывая "будущие" данные от стратегии.
    - **BacktestDataLoader**: Утилита ETL (Extract, Transform, Load).
      Загружает Parquet-файлы, устраняет разрывы в данных и фильтрует сессии.

Структура пакета:
    - `live/`: Модули для работы с сетью (WebSockets, REST wrapper).
    - `backtest/`: Модули для работы с файловой системой и эмуляции.
"""