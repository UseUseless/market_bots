"""
Поставщики рыночных данных (Data Feeds).

Этот пакет отвечает за загрузку, нормализацию и доставку рыночных данных
в ядро системы. Он реализует уровень абстракции, позволяющий стратегиям
работать одинаково как на исторических данных (Backtest), так и в реальном времени (Live).

Состав пакета:
    - **local.py**: Обработчик для бэктестов. Читает исторические данные из локальных
      Parquet-файлов, устраняет разрывы (gaps) и фильтрует торговые сессии.

    - **unified.py**: Универсальный фид для Live-режима. Объединяет загрузку
      истории через REST API и получение обновлений через WebSocket/gRPC.
      Обеспечивает потокобезопасность и расчет индикаторов на лету.

    - **stream_base.py**: Абстрактный базовый класс для всех стриминговых
      подключений (WebSocket).

    - **bybit_stream.py**: Реализация WebSocket клиента для Bybit.
    - **tinkoff_stream.py**: Реализация gRPC стрима для Tinkoff Invest.
"""