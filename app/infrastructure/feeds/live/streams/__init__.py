"""
Адаптеры потоковых подключений (WebSocket/gRPC Streams).

Содержит реализации клиентов для получения обновлений рынка (свечей) в реальном времени.
Эти классы используются внутри `LiveDataProvider` и не должны вызываться напрямую
из бизнес-логики.

Состав:
    - **base.py** (`BaseStreamDataHandler`): Абстрактный интерфейс стрима.
    - **bybit.py**: Реализация WebSocket V5 для Bybit.
    - **tinkoff.py**: Реализация gRPC стрима для Tinkoff Invest API.
"""