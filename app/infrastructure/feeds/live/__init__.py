"""
Компоненты данных реального времени (Real-time Data).

Обеспечивает подачу рыночных данных в работающую стратегию.
Используется оркестратором сигналов (`run_signals.py`).

Состав:
    - **provider.py** (`LiveDataProvider`): Основной класс-координатор.
      Управляет потокобезопасным буфером данных, автоматически рассчитывает
      индикаторы при поступлении новых свечей и синхронизирует REST-историю
      с WebSocket-потоком.
    - **streams/**: Подпакет с реализациями подключений к конкретным биржам.
"""
