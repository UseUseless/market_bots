"""
Модуль исполнения ордеров (Execution Core).

Этот пакет отвечает за превращение абстрактных торговых сигналов в конкретные,
валидные ордера и (в случае бэктеста) за симуляцию их исполнения.

Архитектура исполнения:
1.  **Создание (`order_logic`)**: Класс `OrderManager` принимает сигнал, проверяет риски
    через `RiskManager`, рассчитывает объем через `PositionSizer` и создает `OrderEvent`.
    Это "мозг" исполнения.
2.  **Валидация (`rules`)**: Класс `InstrumentRulesValidator` гарантирует, что рассчитанный
    объем соответствует техническим требованиям биржи (шаг лота, минимальный размер).
3.  **Симуляция (`simulator`)**: Класс `SimulatedExecutionHandler` используется в бэктестах
    для эмуляции сведения ордеров, расчета проскальзывания и комиссий.

Примечание:
    Реальное исполнение ордеров (отправка HTTP-запросов на биржу) находится
    в слое инфраструктуры (`app.infrastructure.execution`), так как оно зависит от конкретного API.
"""