"""
Модуль управления портфелем (Portfolio Core).

Этот пакет является связующим звеном архитектуры. Он объединяет хранение данных (State),
бизнес-логику обработки событий (Manager) и финансовый учет (Accounting).

Основные компоненты:
    - **manager.py**: Класс `Portfolio`. Фасад системы. Принимает внешние события
      (рынок, сигналы, исполнения) и делегирует их обработку специализированным сервисам.
    - **state.py**: Класс `PortfolioState`. Пассивное хранилище данных: текущий капитал,
      список открытых позиций и активных (pending) ордеров.
    - **accounting.py**: Класс `FillProcessor`. "Бухгалтерия". Отвечает за реакцию
      на исполнение ордера: открытие/закрытие позиций и расчет PnL (прибыли/убытков).
"""