"""
Модуль генерации отчетов.

Каждый файл берет рассчитанные метрики и сырые данные сделок и трансформирует их
в удобные для восприятия форматы: текст, таблицы Excel, HTML или графики.

Состав пакета:
    - **console.py**: `ConsoleReportGenerator`. Выводит красивую сводную таблицу
      с результатами бэктеста прямо в терминал (использует `rich`).

    - **excel.py**: `ExcelReportGenerator`. Создает многостраничные файлы `.xlsx`
      с детальной статистикой, условным форматированием и сортировкой. Идеально для
      анализа результатов пакетного тестирования (Batch Backtest).

    - **plot.py**: `PlotReportGenerator`. Визуализирует кривую капитала
      (Equity Curve) и просадки, накладывая график стратегии на график бенчмарка (Buy & Hold).
      Сохраняет результат в `.png`.

    - **wfo.py**: `WFOReportGenerator`. Специфический генератор для Walk-Forward Optimization.
      Сохраняет CSV с шагами обучения, HTML-графики от Optuna и итоговую склеенную эквити.
"""