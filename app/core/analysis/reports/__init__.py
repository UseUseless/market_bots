"""
Модуль генерации отчетов (Reports).

Этот пакет отвечает за уровень представления (Presentation Layer) аналитики.
Его задача — взять рассчитанные метрики и сырые данные сделок и трансформировать их
в удобные для восприятия форматы: текст, таблицы Excel или графики.

Состав пакета:
    - **console_report.py**: `ConsoleReportGenerator`. Выводит красивую сводную таблицу
      с результатами бэктеста прямо в терминал (использует `rich`).

    - **excel_report.py**: `ExcelReportGenerator`. Создает многостраничные файлы `.xlsx`
      с детальной статистикой, условным форматированием и сортировкой. Идеально для
      анализа результатов пакетного тестирования (Batch Backtest).

    - **plot_report.py**: `PlotReportGenerator`. Визуализирует кривую капитала
      (Equity Curve) и просадки, накладывая график стратегии на график бенчмарка (Buy & Hold).
      Сохраняет результат в `.png`.
"""