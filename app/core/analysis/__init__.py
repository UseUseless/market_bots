"""
Модуль аналитики и отчетности (Analysis Core).

Этот пакет отвечает за пост-процессинг результатов торговли (как бэктестов, так и реальной торговли).
Его задача — превратить сырой список сделок в понятные финансовые метрики и визуальные отчеты.

Архитектура аналитики:
1.  **Метрики (`metrics.py`)**: Чистая математика. Классы для расчета коэффициента Шарпа,
    Сортино, максимальной просадки, Profit Factor и других показателей.
2.  **Сессия (`session.py`)**: Оркестратор анализа. Принимает данные одного прогона (сделки + история цен),
    вызывает калькуляторы, синхронизирует временные ряды и запускает генерацию отчетов.
3.  **Сравнение (`comparative.py`)**: Инструменты для агрегации результатов множества прогонов.
    Используется в Дашборде для сравнения стратегий между собой или анализа устойчивости портфеля.
4.  **Отчеты (`reports/`)**: Подсистема визуализации. Отвечает за рендеринг графиков (Matplotlib),
    генерацию Excel-таблиц и вывод итогов в консоль.

Основные компоненты:
    - `AnalysisSession`: Точка входа для анализа одиночного бэктеста.
    - `ComparativeAnalyzer`: Точка входа для сравнительного анализа группы бэктестов.
    - `PortfolioMetricsCalculator`: Ядро математических расчетов эффективности.
"""