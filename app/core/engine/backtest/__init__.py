"""
Модуль движка бэктестинга.

Этот пакет отвечает за симуляцию торговли на исторических данных.
Его цель — воспроизвести условия реального рынка (насколько это возможно)
и проверить, как стратегия вела бы себя в прошлом.

Основные компоненты:
    - **engine.py**: 
      Класс `BacktestEngine`. Реализует основной событийный цикл (Event Loop).
      Он итерируется по истории, шаг за шагом обновляя состояние портфеля и вызывая стратегию.
      Это "сердце" симуляции.
      Класс `BacktestExecutionHandler`. Обрабатывает OrderEvent и FillEvent.
      Симмулириует проскальзывание при исполнении заявок, комиссию 
      и записывает сделки (в т.ч. завершенные)

    - **runners.py**: Функции-оркестраторы.
      Управляют настройкой и запуском тестов (одиночных или пакетных),
      сбором метрик и генерацией отчетов.
"""