"""
Модуль оптимизации стратегий (Optimization Engine).

Этот пакет реализует алгоритм **Walk-Forward Optimization (WFO)**.
В отличие от простой (Grid Search) или генетической оптимизации на всей истории,
WFO имитирует процесс переоптимизации стратегии в реальном времени, периодически
сдвигая окно обучения и тестирования. Это позволяет оценить устойчивость
стратегии к изменению рыночных фаз.

Архитектура процесса:
1.  **Runner (`decorators.py`)**: Точка входа. Настраивает окружение и запускает движок.
2.  **Engine (`engine.py`)**: Оркестратор. Управляет циклом сдвига окна (Rolling Window).
3.  **Preparer (`preparer.py`)**: Загружает и нарезает исторические данные.
4.  **Step Runner (`step_runner.py`)**: Выполняет один шаг WFO (In-Sample оптимизация + Out-of-Sample тест).
5.  **Objective (`objective.py`)**: "Фитнес-функция" для Optuna. Запускает бэктест с заданными параметрами.
6.  **Reporter (`reporter.py`)**: Агрегирует результаты и строит отчеты.

Ключевые зависимости:
    - `optuna`: Библиотека для байесовской оптимизации гиперпараметров.
    - `BacktestEngine`: Используется внутри `Objective` для оценки каждого набора параметров.
"""