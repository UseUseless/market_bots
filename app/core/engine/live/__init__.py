"""
Движок реального времени (Live Engine).

Этот пакет содержит ядро системы для запуска и управления торговыми стратегиями
в режиме реального времени (Live Trading / Signal Monitoring).

В отличие от синхронного движка бэктеста, здесь используется **асинхронный подход**
(библиотека `asyncio`). Это позволяет обрабатывать сотни торговых пар одновременно
в одном процессе, эффективно используя ресурсы при ожидании сетевых событий (I/O bound).

Состав пакета:
    - **loop.py**: Содержит `SignalEngine` — асинхронный менеджер задач.
      Он управляет жизненным циклом отдельных стратегий, обеспечивает их данными
      через `DataFeed` и маршрутизирует сигналы в шину событий.

    - **orchestrator.py**: Высокоуровневый модуль ("Дирижер").
      Отвечает за инициализацию приложения, настройку Dependency Injection,
      запуск адаптеров и синхронизацию состояния движка с базой данных (Hot Reload).
"""