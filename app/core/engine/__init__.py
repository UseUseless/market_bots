"""
Движки исполнения (Execution Engines).

Этот пакет группирует модули, отвечающие за непосредственный запуск и выполнение
торговых стратегий в различных режимах.

Каждый подмодуль оптимизирован под свои специфические задачи:

1.  **backtest** (Синхронный):
    -   Предназначен для проверки стратегий на исторических данных.
    -   Использует простой цикл (`loop`), оптимизированный для последовательной
        обработки больших массивов данных.
    -   Детерминирован (результат всегда одинаков при тех же входных данных).

2.  **live** (Асинхронный):
    -   Предназначен для реальной торговли и мониторинга сигналов.
    -   Построен на `asyncio` для эффективной обработки множества I/O операций
        (сетевые запросы, вебсокеты) в реальном времени.
    -   Поддерживает "горячую" перезагрузку конфигураций.

3.  **optimization** (Параллельный):
    -   Предназначен для подбора параметров (Walk-Forward Optimization).
    -   Использует `multiprocessing` и библиотеку `Optuna` для параллельного
        запуска множества экземпляров бэктестов.
"""