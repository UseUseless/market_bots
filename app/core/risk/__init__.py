"""
Модуль управления рисками (Risk Management Core).

Этот пакет содержит логику защиты капитала. Он отвечает за то, чтобы убытки
в каждой сделке были строго ограничены и соответствовали заданным правилам.

Архитектура риска разделена на три ответственности:
1.  **Определение параметров риска (`manager`)**: Куда ставить Stop Loss? Куда Take Profit?
    Сколько денег мы готовы потерять в этой сделке?
2.  **Расчет объема (`sizer`)**: Сколько лотов/акций можно купить, чтобы при срабатывании
    Stop Loss потерять ровно столько, сколько разрешил менеджер?
3.  **Мониторинг (`monitor`)**: Слежение за открытыми позициями и автоматическая
    генерация ордеров на выход при достижении уровней SL/TP.

Состав пакета:
    - **manager.py**: Абстракция `BaseRiskManager` и реализации (`FixedRiskManager`, `AtrRiskManager`).
    - **sizer.py**: Абстракция `BasePositionSizer` и реализация `FixedRiskSizer`.
    - **monitor.py**: Сервис `RiskMonitor` для пассивной защиты позиций.
"""