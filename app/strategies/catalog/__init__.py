"""
Папка с торговыми стратегиями.

Этот каталог служит хранилищем для всех конкретных алгоритмов торговли.
Система построена на принципе **автоматического обнаружения (Auto-Discovery)**.

Как добавить новую стратегию:
1.  Создайте новый `.py` файл в этой папке (например, `my_super_strat.py`).
2.  Внутри файла создайте класс, наследующий `app.strategies.base_strategy.BaseStrategy`.
3.  Реализуйте обязательные методы (`_calculate_signals`) и атрибуты (`params_config`).

Важно:
    Не добавляйте код импорта стратегий в этот файл (`__init__.py`).
    Загрузчик (`app.strategies.__init__`) сам просканирует эту папку,
    найдет все файлы и загрузит классы стратегий автоматически.
"""