"""
Адаптер Telegram (Notification Adapter).

Этот пакет реализует взаимодействие с Telegram API через библиотеку `aiogram`.
Он выполняет двойную роль в архитектуре:

1.  **Driving Adapter (Первичный):** Принимает команды от пользователей
    (например, `/start`) для управления подписками.
2.  **Driven Adapter (Вторичный):** Отправляет уведомления о торговых сигналах
    пользователям.

Особенности реализации:
    - Поддержка мульти-бот архитектуры (одно приложение управляет множеством ботов).
    - Динамическое управление (Hot Reload) ботов без перезапуска процесса.

Состав пакета:
    - **manager.py**: `BotManager` — оркестратор, управляющий жизненным циклом
      экземпляров `aiogram.Bot` и задачами поллинга.
    - **publisher.py**: `TelegramSignalSender` — слушатель шины событий, который
      маршрутизирует сигналы стратегий конкретным подписчикам через менеджера.
"""