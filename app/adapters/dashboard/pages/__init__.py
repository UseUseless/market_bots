"""
Страницы Дашборда (Dashboard Pages).

Этот пакет содержит отдельные страницы многостраничного приложения Streamlit.
Фреймворк Streamlit автоматически сканирует эту директорию и добавляет
найденные файлы в навигационную панель (Sidebar).

Архитектурные особенности:
    - **Naming Convention:** Имена файлов начинаются с цифр (`1_`, `2_`) для
      управления порядком сортировки в меню навигации.
    - **State Management:** Каждая страница работает как независимый скрипт,
      но разделяет общий `st.session_state` и кэш данных (`st.cache_data`).
    - **Direct DB Access:** Страницы используют прямой доступ к SQLite для чтения
      состояния системы, что реализует паттерн CQRS (Command Query Responsibility Segregation)
      в упрощенном виде: ядро пишет (Write), дашборд читает (Read).

Состав пакета:
    - **1_Live_Monitor.py**: Панель мониторинга реального времени.
      Отображает KPI, ленту последних сигналов и статус активности ботов.

    - **2_Configuration.py**: Панель администрирования.
      Интерфейс CRUD (Create, Read, Update, Delete) для управления ботами
      и конфигурациями торговых стратегий.
"""