"""
Модуль инициализации (Bootstrap Layer).

Этот пакет отвечает за начальную настройку и "сборку" приложения.
Здесь реализуется паттерн **Dependency Injection (DI)**.

Вместо того чтобы компоненты сами создавали свои зависимости (например,
стратегия сама подключалась к БД), они получают их извне.
Пакет `bootstrap` — это то место, где эти связи создаются.

Состав пакета:
    - **container.py**: Содержит класс `Container`, который управляет
      созданием и хранением глобальных сервисов (Singletons), таких как
      подключения к биржам, менеджер ботов и шина событий.
"""