"""
Главная точка входа в приложение (Launcher).

Этот скрипт запускает интерактивное CLI-меню, которое служит "пультом управления"
для всей системы. Через него пользователь получает доступ к:
- Загрузке рыночных данных.
- Запуску бэктестов и оптимизации.
- Мониторингу сигналов в реальном времени.
- Настройке конфигурации (боты, БД).

Скрипт настраивает окружение (`sys.path`) и передает управление
адаптеру командной строки (`app.adapters.cli`).

Запуск:
    python launcher.py
"""

import sys
import os

# Добавляем текущую директорию (корень проекта) в начало sys.path.
# Это гарантирует, что интерпретатор сможет найти пакет 'app',
# даже если скрипт запущен из другой директории (хотя для launcher.py это редкость).
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from app.adapters.cli.launcher_logic import main


if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        # Обработка нажатия Ctrl+C для чистого выхода без Traceback
        print("\n👋 Программа завершена пользователем.")
        sys.exit(0)